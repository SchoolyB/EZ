module github.com/marshallburns/ez/cmd/wasm

go 1.23.1

// This module is frozen — it depends on the v2.x interpreter packages
// which have been removed from the main module. The pre-built ez.wasm
// binary still works. This will be ported to Emscripten in the future.
