/*
 * Error Test: E12001 - map-requires-map
 * Expected: "requires a map argument"
 */

import @maps

do main() {
    mut arr [int] = {1, 2, 3}
    mut keys [int] = maps.keys(arr)  // Passing array to map function
}
