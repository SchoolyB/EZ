/*
 * Error Test: E12002 - map-immutable
 * Expected: "immutable" or "cannot modify"
 */

import @maps

do main() {
    const m map[string:string] = {"key": "value"}
    maps.set(m, "new_key", "new_value")  // cannot modify const map
}
