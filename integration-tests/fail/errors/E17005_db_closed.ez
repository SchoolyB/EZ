/*
 * Error Test: E17005 - db-closed
 * Expected: "operation on closed database"
 */

 import @db

 do main() {
    mut store, _ = db.open("mydb.ezdb")
    mut _ = db.close(store)
    db.set(store, "name", "Alice")
 }
