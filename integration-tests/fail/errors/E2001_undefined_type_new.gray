/*
 * Error Test: E2001 - undefined type in new expression
 * Expected: E2001 unexpected symbol (parser expects new(Type) syntax)
 */

do main() {
    mut p = new FooBar  // FooBar type not defined
}
