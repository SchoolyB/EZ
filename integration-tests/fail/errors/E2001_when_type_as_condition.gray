/*
 * Error Test: E2001 - when condition must be a value, not a type name
 * Expected: compiler should reject this with E2001
 */

const COLOR enum {
    RED
    GREEN
    BLUE
}

do main() {
    when COLOR {
        is COLOR.RED { println("Red") }
        default { println("Wrong") }
    }
}
