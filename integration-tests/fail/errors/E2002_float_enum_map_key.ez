/*
 * Error Test: E2002 - float-enum-map-key
 * Expected: "float-based enum cannot be used as map key"
 */

enum Status [float] {
    ACTIVE = 1.0,
    INACTIVE = 2.0
}

do main() {
    mut m map[Status: string] = {}  // float enum as map key
}

