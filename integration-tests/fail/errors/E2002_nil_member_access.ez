/*
 * Error Test: E4010 - nil-member-access
 * Expected: "cannot access member of nil"
 */

def Person struct {
    name string
}

do getNull() -> Person {
    give nil
}

do main() {
    mut p Person = getNull()
    mut name string = p.name  // Accessing member on nil
}
