/*
 * Error Test: E2002 - undefined-enum-value
 * Expected: "enum value" or "not found"
 */

enum Color {
    RED,
    GREEN,
    BLUE
}

do main() {
    mut c = Color.YELLOW  // YELLOW does not exist
}
