/*
 * Error Test: E2002 - undefined enum value (uses old 'enum' syntax)
 * Expected: E2002 unexpected symbol
 */

enum Status {
    ACTIVE,
    INACTIVE
}

do main() {
    mut s = Status.PENDING  // PENDING does not exist
}
