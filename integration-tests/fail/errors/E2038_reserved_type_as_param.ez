/*
 * Error Test: E2038 - reserved type name used as parameter name
 * Expected: "'bool' is a reserved type name" error
 */

do check(bool int) -> int {
    return bool
}

do main() {
    mut x = check(1)
}
