// E2044: float type not allowed in when statement
import @std
using std

do main() {
    temp f float = 3.14
    when f {
        is 1.0 { println("one") }
        is 2.0 { println("two") }
        default { println("other") }
    }
}
