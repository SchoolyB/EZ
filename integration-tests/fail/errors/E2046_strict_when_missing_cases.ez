import @std
using std

const Color enum {
    RED
    GREEN
    BLUE
}

do main() {
    temp c Color = Color.RED

    #strict
    when c {
        is Color.RED {
            println("red")
        }
        // Missing GREEN and BLUE
    }
}
