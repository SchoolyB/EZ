/* Error Test: E3001 - passing [int] to function expecting [string] */
do join(arr [string]) -> string {
    mut result string = ""
    for_each s in arr { result = "${result}${s}" }
    return result
}
do main() {
    mut a [int] = {1, 2, 3}
    println(join(a))
}
