/*
 * Error Test: E3001 - when/is type mismatch int vs string
 * Expected: "type mismatch in 'when'"
 */

do main() {
    mut x int = 5
    when x {
        is "hello" { }
        default { }
    }
}
