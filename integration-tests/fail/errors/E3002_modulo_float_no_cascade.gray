/*
 * Error Test: E3002 - modulo on float produces exactly one error
 *
 * Regression for #1488: modulo on float also went through the
 * cascade-prone path. Now collapses to TK_UNKNOWN after the
 * op-level error.
 */

do main() {
    mut x float = 3.14 % 2.0
}
