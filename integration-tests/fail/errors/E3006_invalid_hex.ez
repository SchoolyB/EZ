/*
 * Error Test: E3006 - invalid-hex
 * Expected: "invalid hex"
 */

import @encoding

do main() {
    // Invalid hex string (contains non-hex characters)
    mut result string, err Error = encoding.hex_decode("GHIJ")
    if err != nil {
        panic("${err}")
    }
}
