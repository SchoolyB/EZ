/*
 * Error Test: E13002 - json-unsupported-type
 * Expected: "unsupported value" or "failed to encode"
 */

import @json, @math
using math

do main() {
    // Infinity cannot be represented in JSON
    mut result, err = json.encode(math.INF)
    if err != nil {
        panic("${err}")
    }
}
