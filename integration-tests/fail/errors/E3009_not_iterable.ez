/*
 * Error Test: E3009 - not-iterable
 * Expected: "cannot iterate over type 'int'; for_each requires an array, map, or string"
 */

do main() {
    mut x int = 5
    for_each item in x {  // Cannot iterate over an integer
        println(item)
    }
}
