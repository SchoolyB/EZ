/*
 * Error Test: E3015 - call-non-function
 * Expected: "cannot call" or "non-function"
 */

import @std
using std

do main() {
    mut x int = 5
    mut y = x()  // Should fail: calling non-function
}
