/*
 * Error Test: E3017 - fmt.printf cannot format pointer
 * Expected: "cannot format value of type"
 */

import @fmt

do main() {
    mut x int = 42
    mut p ^int = addr(x)
    fmt.printf("%s", p)
}
