/*
 * Error Test: E3018 - when condition cannot be an array or map
 * Expected: compiler should reject this with E3018
 */

do main() {
    mut arr [int] = {1, 2, 3}
    when arr {
        is 1 { println("match") }
        default { println("no match") }
    }
}
