/*
 * Error Test: E17002 - db-read-failed
 * Expected: "failed to read database file"
 */

 import @db, @io

 do main() {
    mut _, _ = io.write_file("mydb.ezdb", "{}", io.WRITE_ONLY)
    mut _, err = db.open("mydb.ezdb") // trying to read a write only file
 }
