/*
 * Error Test: E7003 - db.sort invalid order constant
 * Expected: "invalid order constant"
 */

import @db

do main() {
    mut store, _ = db.open("test.ezdb")
    db.sort(store, 999)  // Invalid sort order constant
}
