/*
 * Error Test: E12003 - map-key-not-found
 * Expected: "key not found" or "not found in map"
 */

import @maps

do main() {
    mut m = maps.new()
    maps.set(m, "key", "value")
    mut v = maps.get(m, "nonexistent")  // key not found
}
