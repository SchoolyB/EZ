/*
 * Error Test: E7006 - requires-function
 * Expected: "must be a function" or "requires function"
 */

import @arrays

do main() {
    mut arr [int] = {1, 2, 3}
    mut result = arrays.map(arr, "not a function")
}
