/*
 * Error Test: E3038 - typeof() with void struct-namespaced function
 * Expected: "void" and "typeof"
 */

const Foo struct {
    x int

    do doNothing() {
        return
    }
}

do main() {
    typeof(Foo.doNothing())
}
