/*
 * Error Test: E3038 - void function call passed to print
 *
 * Regression for #1483: same check as println, exercised against
 * the print builtin to confirm the fix covers the whole family.
 */

do nothing() {}

do main() {
    print(nothing())
}
