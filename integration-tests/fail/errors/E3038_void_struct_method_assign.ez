/*
 * Error Test: E3038 - assigning void struct-namespaced function result
 * Expected: "void" and "cannot assign"
 */

const Foo struct {
    x int

    do doNothing() {
        return
    }
}

do main() {
    mut x = Foo.doNothing()
}
