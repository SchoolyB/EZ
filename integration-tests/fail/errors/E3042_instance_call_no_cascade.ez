/*
 * Error Test: E3042 - instance.func() gives helpful error without cascading
 * Expected: E3042 "use 'Point.scale()' instead" error, no false E3038/E3041
 */

const Point struct {
    x int
    y int

    do scale(factor int) -> int {
        return 0
    }
}

do main() {
    mut p = Point{x: 1, y: 2}
    mut r = p.scale(2)
}
