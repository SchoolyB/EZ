/*
 * Error Test: E3043 - cannot-convert-array
 * Expected: "cannot convert [int] to int; only numeric types, strings, and bools can be converted"
 */

do main() {
    mut arr [int] = {1, 2, 3}
    mut x int = int(arr)  // cannot convert array to int
}
