/*
 * Error Test: E3043 - cannot cast pointer to int
 * Expected: E3043 "cannot cast" error
 */

const Point struct {
    x int
    y int
}

do main() {
    mut p = new(Point)
    mut n int = cast(p, int)
}
