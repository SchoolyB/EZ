/*
 * Error Test: E3043 - cannot cast struct to int
 * Expected: "cannot cast"
 */

const Point struct {
    x int
    y int
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    mut n int = cast(p, int)
}
