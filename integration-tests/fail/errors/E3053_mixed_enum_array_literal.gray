/*
 * Error Test: E3001 - mixed enum types in array literal
 * Expected: type mismatch for different enum in array
 */

const Color enum {
    RED
    GREEN
}

const Dir enum {
    NORTH
    SOUTH
}

do main() {
    mut arr [Color] = {Color.RED, Dir.NORTH}
}
