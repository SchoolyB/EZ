/*
 * Error Test: E3060 - wildcard return with no wildcard params
 * Expected: "wildcard '?' in return type cannot be resolved" error
 */

do foo() -> ? {
    return 10
}

do main() {
    println("should not compile")
}
