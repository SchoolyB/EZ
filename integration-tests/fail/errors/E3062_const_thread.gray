/*
 * Error Test: E3062 - const on a thread handle
 *
 * Regression for #1480: thread handles are inherently stateful
 * (join mutates internal state).
 */

import @threads

do worker() {}

do main() {
    const t = threads.spawn(()worker)
}
