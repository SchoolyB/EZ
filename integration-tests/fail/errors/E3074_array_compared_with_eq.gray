/*
 * Error Test: E3074 - array-comparison
 * Expected: "arrays cannot be compared with comparison operators"
 */

do main() {
    mut a [int] = {1, 2, 3}
    mut b [int] = {1, 2, 3}
    if a == b {
        println("equal")
    }
}
