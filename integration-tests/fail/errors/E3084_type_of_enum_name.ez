/*
 * Error Test: E3084 - type_of() with an enum name instead of a value
 * Expected: E3084 "type_of() expects a value, not a type name"
 * Covers: #1586
 */

const Color enum {
    RED
    GREEN
    BLUE
}

do main() {
    println(type_of(Color))
}
