/*
 * Error Test: E3089 - can-fail stdlib function result not fully handled
 * Expected: error on single-var assignment of can-fail stdlib function
 */

import @io

do main() {
    mut content = io.read_file("/tmp/test.txt")
    println(content)
}
