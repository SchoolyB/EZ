/*
 * Error Test: E3099 - reserved stdlib type name used as struct name
 * Regression test for #1791: naming a user struct with any of the 10 reserved
 * stdlib opaque type names caused a silent C compilation failure with no EZ
 * diagnostic. Expected: E3099
 */

const Router struct {
    name string
}

do main() {
    mut r = new(Router)
    println(r.name)
}
