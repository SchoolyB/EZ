/*
 * Error Test: E3117 - enum compared with integer type
 * Expected: cannot compare enum with int
 */

const Color enum {
    RED
    GREEN
    BLUE
}

do main() {
    mut c Color = .RED
    mut x int = 0
    if c == x { println("bad") }
}
