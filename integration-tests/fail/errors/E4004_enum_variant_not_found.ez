/*
 * Error Test: E4004 - enum-variant-not-found
 * Expected: "not found" or "Yellow"
 */


const Color enum {
    Red
    Green
    Blue
}

do main() {
    mut c = Color.Yellow  // Should fail: Yellow doesn't exist
}
