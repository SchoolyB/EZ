/*
 * Error Test: E4005 - string-index-out-of-bounds
 * Expected: "string index" or "out of bounds"
 */

import @strings

do main() {
    mut s string = "hello"
    mut c = strings.char_at(s, 100)  // index out of bounds
}
