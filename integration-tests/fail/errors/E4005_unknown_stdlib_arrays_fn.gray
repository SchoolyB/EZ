/*
 * Error Test: E4005 - typo'd call on the @arrays stdlib module
 *
 * Regression for #1497: same bug class as the @strings case,
 * different module.
 */

import @arrays

do main() {
    mut a [int] = {1, 2, 3}
    arrays.bogus(a)
}
