/*
 * Error Test: E4005 - typo'd call on the @mem stdlib module
 *
 * The fix also covers the duplicate mem/sqlite
 * dispatch blocks that existed in the typechecker.
 */

import @mem

do main() {
    mut a = mem.arena(1024)
    mem.bogus(a)
}
