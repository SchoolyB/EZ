/*
 * Error Test: E4016 - undefined-param-type
 * Expected: "undefined type 'UnknownType'"
 */

do process(data UnknownType) {  // Unknown parameter type
    println(data)
}

do main() {
    process("test")
}
