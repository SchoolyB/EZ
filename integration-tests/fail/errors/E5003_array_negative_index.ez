/*
 * Error Test: E5003 - negative array index
 * Expected: "negative"
 */

import @std
using std

do main() {
    temp arr [int] = {1, 2, 3}
    temp x = arr[-1]  // Negative index is always invalid
    println(x)
}
