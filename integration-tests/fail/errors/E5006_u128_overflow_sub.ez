/*
 * Error Test: E5006 - integer-overflow (u128 subtraction)
 * Expected: "integer overflow"
 * Tests that u128 underflows (goes negative)
 */

do main() {
    mut zero u128 = 0
    mut result u128 = zero - 1  // Underflow (unsigned can't go negative)
}
