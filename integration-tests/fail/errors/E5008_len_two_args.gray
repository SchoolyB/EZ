/*
 * Error Test: E5008 - len() called with too many arguments
 *
 * arity check rejects len() with more than
 * one argument.
 */

do main() {
    mut a [int] = {1, 2}
    mut b [int] = {3, 4}
    println(len(a, b))
}
