/*
 * Error Test: E5008 - too few args for function with default params
 * Expected: "expects 1-2 argument(s), got 0"
 */

do greet(name string, greeting string = "Hello") -> string {
    return "${greeting}, ${name}!"
}

do main() {
    mut s string = greet()
}
