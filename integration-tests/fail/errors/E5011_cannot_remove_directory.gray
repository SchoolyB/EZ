/*
 * Error Test: E5011 - cannot-remove-directory
 * Expected: "cannot remove directories" or "io.remove"
 */

import @io

do main() {
    io.delete_file("/tmp")  // io.delete_file cannot remove directories
}
