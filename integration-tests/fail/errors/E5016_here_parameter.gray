/*
 * Error Test: E5016 - 'here' used as a function parameter name
 *
 * A parameter named 'here' would shadow the builtin inside the
 * function body. Rejected to keep the builtin always callable.
 */

do f(here int) {
    println("${here}")
}

do main() {
    f(1)
}
