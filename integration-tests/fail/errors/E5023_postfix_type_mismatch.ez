/*
 * Error Test: E5023 - postfix-type-mismatch
 * Expected: "cannot use '++' on type 'string'; only integer types support increment/decrement"
 */


do main() {
    mut s string = "hello"
    s++  // Should fail: increment on string
}
