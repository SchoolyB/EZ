/*
 * Error Test: E5024 - return-type-mismatch (runtime)
 * Expected: "return type mismatch"
 */


do get_value() -> uint {
    mut x int = -1
    return x  // Runtime error: int -1 cannot be returned as uint
}

do main() {
    mut val uint = get_value()
    println(val)
}
