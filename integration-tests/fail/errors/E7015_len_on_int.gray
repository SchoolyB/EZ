/*
 * Error Test: E7015 - len() on an int value
 *
 * Regression for #1490: len() now whitelists only string/array/map
 * receivers and rejects primitives with a type-named error.
 */

do main() {
    println(len(42))
}
