/*
 * Error Test: E9001 - array-pop-empty
 * Expected: "cannot pop from empty array"
 */

import @arrays

do main() {
    mut arr [int] = {}
    mut x int = arrays.pop(arr)  // Pop from empty array
}
