/*
 * Error Test: E9002 - array-non-numeric
 * Expected: "numeric" or "array"
 */

import @arrays

do main() {
    mut arr [string] = {"a", "b", "c"}
    mut total = arrays.sum(arr)  // sum requires numeric array
}
