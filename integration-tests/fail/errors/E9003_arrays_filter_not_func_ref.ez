/*
 * Error Test: E9003 - arrays.filter() requires a function reference
 * Expected: E9003 error
 */

import @arrays

do main() {
    mut nums [int] = {1, 2, 3}
    mut result [int] = arrays.filter(nums, 42)
}
