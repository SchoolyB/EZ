/*
 * Panic Test: P0019 - cast int to uint where value is negative
 *
 * cast(int_val, uint) must panic at runtime when the int value is negative
 * rather than silently wrapping to a large uint.
 */

do main() {
    mut n int = -1
    mut u uint = cast(n, uint)
    println(u)
}
