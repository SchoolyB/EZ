import @server

do home(req HttpRequest) -> HttpResponse {
    return server.text(200, "hello")
}

do main() {
    mut r = server.add_router()
    server.cors(r, "evil.com\r\nX-Admin: true")
    server.add_route(r, "GET", "/", ()home)
}
