/*
 * Error Test: integer-overflow (int += overflow)
 * Expected: "addition result is too large"
 * Compound += on plain int must overflow-check, not silently wrap.
 */

do main() {
    mut x int = 9223372036854775807  // INT64_MAX
    x += 1
}
