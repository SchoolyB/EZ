/*
 * Error Test: integer-overflow (int /= overflow)
 * Expected: "division result is too large"
 * INT64_MIN /= -1 has no positive representation.
 */

do main() {
    mut x int = -9223372036854775808
    mut y int = -1
    x /= y
}
