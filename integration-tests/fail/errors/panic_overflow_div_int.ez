/*
 * Error Test: integer-overflow (int_min / -1)
 * Expected: "division result is too large"
 * TYPE_MIN / -1 has no positive representation; must panic, not UB.
 */

do main() {
    mut x int = -9223372036854775808  // min i64
    mut y int = -1
    mut r int = x / y  // Overflow
}
