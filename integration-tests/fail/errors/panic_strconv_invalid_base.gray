/*
 * Error Test: strconv.to_int panics on invalid base at runtime
 * Expected: runtime panic (base 0 passed via variable)
 */

import @strconv

do main() {
    mut base int = 0
    mut x int = strconv.to_int("42", base)
}
