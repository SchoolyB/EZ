/*
 * Error Test: E3001 - struct function arg mismatch with using
 * Expected: error message shows "Foo" not "utils_Foo"
 */

import "./utils.ez"
using utils

do main() {
    mut f = new(Foo)
    f.take_foo(42)
}
