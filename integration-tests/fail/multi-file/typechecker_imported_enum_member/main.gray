
// Test: Invalid enum member access with imported enum
// Expected: E4004 - enum has no member 'RUNNING'

import lib"./lib.gray"

do main() {
    // ERROR: 'RUNNING' is not a member of Status enum
    mut s = lib.Status.RUNNING
    println(s)
}
