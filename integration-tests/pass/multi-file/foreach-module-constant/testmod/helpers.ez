
import @std
using std

do print_items() {
    for_each item in ITEMS {
        println(item)
    }
}

do sum_numbers() -> int {
    mut total int = 0
    for_each n in NUMBERS {
        total += n
    }
    return total
}
