import and use "./lib.ez"

do main() {
    mut result string = greet("Unqualified")
    if result == "Hello, Unqualified" && VERSION == "1.0.0" {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
