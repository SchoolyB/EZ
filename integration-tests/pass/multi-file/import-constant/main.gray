import "./lib.gray"

do main() {
    mut result string = lib.greet("World")
    if result == "Hello, World" && lib.VERSION == "1.0.0" {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
