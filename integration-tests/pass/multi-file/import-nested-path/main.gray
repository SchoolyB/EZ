import "./deep/nested/util.ez"

do main() {
    mut result string = util.deep_func()
    if result == "deep" {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
