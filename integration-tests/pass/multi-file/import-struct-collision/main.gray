import "./geo.ez"
import "./color.ez"

do main() {
    mut g = geo.Point{x: 1, y: 2}
    mut c = color.Point{r: 255, g: 128, b: 0}
    if g.x == 1 && g.y == 2 && c.r == 255 && c.b == 0 {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
