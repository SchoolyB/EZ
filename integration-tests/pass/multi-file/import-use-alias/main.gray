import and use mylib "./lib.gray"

do main() {
    mut a string = greet("Both")
    mut b string = mylib.greet("Qualified")
    if a == "Hello, Both" && b == "Hello, Qualified" {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
