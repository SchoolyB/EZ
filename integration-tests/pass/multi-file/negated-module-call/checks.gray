/*
 * checks.ez - User module with bool-returning functions
 */


do is_positive(n int) -> bool {
    return n > 0
}

do is_empty(s string) -> bool {
    return len(s) == 0
}

do is_in_range(val int, low int, high int) -> bool {
    return val >= low && val <= high
}
