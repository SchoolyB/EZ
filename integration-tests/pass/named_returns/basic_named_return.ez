/* Test: Basic named return variable
 * Expected: passes with output "Alice"
 */

import @std
using std

do getUserName() -> (name string) {
    name = "Alice"
    return name
}

do main() {
    println(getUserName())
}
