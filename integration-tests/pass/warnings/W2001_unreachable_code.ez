/*
 * Warning Test: W2001 - unreachable-code
 * Expected: warning about code that will never execute
 */


do helper() -> int {
    return 42
    mut x int = 10  // This is unreachable
}

do main() {
    mut val int = helper()
    println(val)
}
