/*
 * Warning Test: W2003 - unreachable code after return inside if block
 * Expected: warning about unreachable statement after return
 */


do check(x int) -> int {
    if x > 10 {
        return x
        mut y int = 5  // unreachable inside if block
    }
    return 0
}

do main() {
    println(check(20))
}
