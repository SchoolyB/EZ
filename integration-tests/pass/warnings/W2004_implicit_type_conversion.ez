/*
 * Warning Test: W2004 - implicit-type-conversion
 * Expected: warning about implicit type conversion
 */

import @std
using std

do main() {
    mut x byte = 100
    mut y int = 50
    mut z int = x + y  // byte implicitly promoted to int
    println(z)
}
