/*
 * Warning Test: W2008 - integer-overflow-potential
 * Expected: warning about integer arithmetic overflow potential
 */

import @std
using std

do main() {
    temp x i8 = 120
    temp y i8 = 10
    temp z = x + y  // 130 overflows i8 range (-128 to 127)
    println(z)
}
