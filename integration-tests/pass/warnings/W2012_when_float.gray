/*
 * Warning Test: W2012 - when-condition-is-float
 * Expected: warning that float equality in when is imprecise
 */

do main() {
    mut x float = 0.5
    when x {
        is 0.5 { println("half") }
        default { println("other") }
    }
}
