// Test @random module error cases
// This file should produce an error

import @std, @random

using std

do main() {
    // Error: random.choice() on empty array
    temp empty [int] = {}
    temp x int = random.choice(empty)  // Should error
    println(x)
}
